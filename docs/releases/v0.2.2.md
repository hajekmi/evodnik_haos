Fixes HTTP 500 when opening the eVodnik setup or reconfiguration form.

The form now uses field types Home Assistant can serialize for its frontend.
Custom host, listener address, and MQTT prefix validation runs after submission
and reports errors next to the relevant fields. Valid values are normalized
before being saved.

HTTP regression tests cover opening setup and reconfiguration, invalid input,
and saving valid configuration through Home Assistant's actual config-flow API.
The setup test reproduced the HTTP 500 before the fix.

Update to **v0.2.2** through HACS, restart **Home Assistant Core**, and add the
integration again. Existing configuration does not need to be removed.
Requires Home Assistant Core 2026.9 or newer.
